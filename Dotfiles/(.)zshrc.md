```
# ===========================================================================
# ===========================================================================
# ======================| [1] PACKAGES CONFIGURATION |=======================

# ---[1.1] ZSH | Oh My Zsh

# -------[1.1.A] Install Path & Theme:
export ZSH="/usr/share/oh-my-zsh"
ZSH_THEME=""
# -------[1.1.B] Plugins:
if [ -d /usr/share/zsh/site-functions ]; then
  fpath=(/usr/share/zsh/site-functions $fpath)
fi
plugins=(sudo command-not-found zsh-autosuggestions zsh-syntax-highlighting)
# -------[1.1.C] Load:
autoload -U compinit && compinit
source $ZSH/oh-my-zsh.sh

# \~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~/

# ---[1.2] ZSH | Shell

# -------[1.2.A] Preferred Editor:
export EDITOR='nano'
# -------[1.2.B] Completion Waiting Dots:
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# COMPLETION_WAITING_DOTS="true"

# \~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~/

# ---[1.3] ZSH | POWERLEVEL10K

# -------[2.1.A] Custom P10K:
source /usr/share/zsh-theme-powerlevel10k/powerlevel10k.zsh-theme
# -------[1.3.A] Custom Prompt:      (Run 'p10k configure' | Edit '~/.p10k.zsh')
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

# ===========================================================================
# ===========================================================================
# ========================| [2] USER CONFIGURATION |=========================

# ---[2.1] TU_USUARIO | Custom Aliases

# -------[2.1.A] Terminal Interface:
alias ft="fastfetch"
alias p="pacseek"

alias ls="lsd"
alias la="lsd -a"
alias lt="lsd -a --tree --depth=1"
# alias cd="y"
alias grep="rg"
alias ps="procs"
alias cat="bat"
alias find="fd"

alias du="dust"
alias df="duf"
alias top="btop"
alias ping="gping"

alias c="clear"
alias e="exit"
# -------[2.1.B] Applications:
alias riseup-vpn="env QT_QUICK_BACKEND=software /usr/bin/riseup-vpn"

# \~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~/

# ---[2.2] TU_USUARIO | Custom Apps

# -------[2.2.A] YAZI:
function y() {
 local tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
 yazi "$@" --cwd-file="$tmp"
 if cwd="$(cat "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
  builtin cd "$cwd"
 fi
 rm -f "$tmp"
}
# -------[2.2.B] Custom Clear:
clear() {
	command clear
	fastfetch
}
# -------[2.2.C] Zoxide:
eval "$(zoxide init zsh)"
# -------[2.2.D] Fastfetch:
ft

# ===========================================================================
# ===========================================================================
# ===========================================================================
```